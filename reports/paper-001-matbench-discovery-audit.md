# ReproLab Paper-001 — Independent Reproducibility Audit of Matbench Discovery

_Placeholder — this report is generated by `scripts/make_report.py` after Layer A runs._

**Scope.** Verify Matbench Discovery leaderboard stability metrics independently.
Layer A (CPU): recompute metrics from published predictions vs the official YAML.
Layer B (GPU): regenerate predictions and re-score.

**Status:** Layer A was executed for CHGNet (`unique_prototypes` + `full_test_set`).

**Finding:** MATCH. Independent recompute and upstream `stable_metrics` both reproduce
the official YAML metrics exactly (integer confusion counts identical; F1/MAE/RMSE/R²
equal to 3 dp). Side finding retained: the `pred_file_url` (`figshare.com/files/…`) is
WAF-blocked for naive downloaders — the Figshare API endpoint is required; and the
PyPI wheel `1.3.1` ships a different module layout than the repo HEAD `1.3.1`.

**Next:** Run the same Layer A check for a second model (MACE / SevenNet / ORB).

See `papers/matbench-discovery/{metric_check.md,reproduction_plan.md,failure_notes.md}`.

<!-- Run `python scripts/make_report.py` to populate this file from artifacts. -->
